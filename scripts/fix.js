const fs = require('fs'); let html = fs.readFileSync('public/index.html', 'utf8'); const st = html.indexOf('function isTyping()'); const en = html.indexOf('requestAnimationFrame(frame);'); if (st > 0 && en > st) { const startCut = html.lastIndexOf('<script>', st); const endCut = html.indexOf('</script>', en) + 9; html = html.substring(0, startCut) + '<script src=\"game.js\"></script>' + html.substring(endCut); fs.writeFileSync('public/index.html', html); console.log('Fixed'); } else { console.log('Not found'); }
